# Passwort fuer das Dashboard (Browser-Login, mindestens 12 Zeichen).
# Erzeugen: openssl rand -hex 20
DASHBOARD_PASSWORD=

# Optional: Euler-Stream-API-Key fuer den TikTok-Sign-Server (hoeheres Rate-Limit).
TIKTOK_SIGN_API_KEY=
